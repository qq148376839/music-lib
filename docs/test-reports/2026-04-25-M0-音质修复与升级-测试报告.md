# 测试报告: M0 音质修复与升级

> 日期: 2026-04-25
> 设计文档: docs/2026-04-25-音质修复与升级.md
> 测试范围: download/, model/, internal/api/

---

## 测试执行结果

### go build

```
go build ./...  →  通过（无错误）
```

### go vet

```
go vet ./download/... ./model/... ./internal/api/... ./qq/... ./netease/... ./kugou/...  →  通过（无警告）
```

### go test

| 包 | 结果 | 覆盖率 | 新增测试数 |
|----|------|--------|-----------|
| download | PASS | 38.4% | 15（qualityScore 10 + WriteSongToDisk 5） |
| model | PASS | 26.7% | 9（QualityString） |
| internal/api | 无测试文件 | - | - |

全部 **24 个新增测试通过**。

---

## 测试用例清单

### model/song_test.go — QualityString()

| 用例 | 输入 | 期望 | 结果 |
|------|------|------|------|
| Flac | Ext="flac" | "FLAC" | PASS |
| Wav | Ext="wav" | "WAV" | PASS |
| MP3 320k | Ext="mp3", Bitrate=320 | "320kbps MP3" | PASS |
| MP3 128k | Ext="mp3", Bitrate=128 | "128kbps MP3" | PASS |
| M4A 96k | Ext="m4a", Bitrate=96 | "96kbps M4A" | PASS |
| MP3 无码率 | Ext="mp3", Bitrate=0 | "MP3" | PASS |
| 空 | Ext="", Bitrate=0 | "" | PASS |
| FLAC 忽略码率 | Ext="flac", Bitrate=1411 | "FLAC" | PASS |
| 大小写不敏感 | Ext="FLAC" | "FLAC" | PASS |

### download/writer_test.go — qualityScore()

| 用例 | 输入 | 期望 | 结果 |
|------|------|------|------|
| FLAC | ext="flac" | 1000 | PASS |
| WAV | ext="wav" | 1000 | PASS |
| MP3 320k | ext="mp3", br=320 | 320 | PASS |
| MP3 128k | ext="mp3", br=128 | 128 | PASS |
| MP3 未知大文件 | ext="mp3", size=10MB | 300 | PASS |
| MP3 未知中文件 | ext="mp3", size=5MB | 200 | PASS |
| MP3 未知小文件 | ext="mp3", size=1MB | 100 | PASS |
| M4A 高码率 | ext="m4a", br=256 | 250 | PASS |
| M4A 低码率 | ext="m4a", br=96 | 90 | PASS |
| 未知格式 | ext="ogg" | 50 | PASS |
| 排序正确性 | flac > mp3_320 > mp3_128 > m4a | 验证链式比较 | PASS |

### download/writer_test.go — WriteSongToDisk()

| 用例 | 场景 | 期望 Action | 结果 |
|------|------|------------|------|
| ActionNew | 目录下无文件 | ActionNew | PASS |
| ActionSkipped | 已有 FLAC，下载 MP3 128k | ActionSkipped | PASS |
| ActionUpgraded | 已有 MP3 3MB，下载 FLAC | ActionUpgraded + 旧文件删除 | PASS |
| SameFormatSameBitrate | 已有 MP3 5MB，下载 MP3 128k | ActionSkipped | PASS |
| LyricsAlwaysSaved | 已有 FLAC + 跳过，但传了歌词 | ActionSkipped + .lrc 文件存在 | PASS |

---

## 设计文档验收标准覆盖

| 验收标准（设计文档 9.3 节） | 测试覆盖 | 状态 |
|---------------------------|---------|------|
| 下载歌曲后 Task JSON 包含 actual_quality 且非空 | 需集成测试（Provider + Manager 联动）| 未覆盖（单元测试层面已验证 QualityString） |
| 无损降级时前端显示橙色"已降级" | 前端逻辑，无自动化测试 | 需手动验证 |
| 历史任务有"升级"按钮 | 前端逻辑 | 需手动验证 |
| "全部升级"按钮入队所有任务 | 需集成测试（Manager.EnqueueUpgrade）| 未覆盖 |
| 升级替换后旧文件删除、新文件存在 | TestWriteSongToDisk_ActionUpgraded | **已覆盖** |
| 升级中断旧文件完好 | 需模拟下载失败场景 | 未覆盖（但 .tmp 机制逻辑简单） |
| 已有 FLAC 重下载 → 跳过 | TestWriteSongToDisk_ActionSkipped | **已覆盖** |
| Favicon 显示 | 需手动验证 | 需手动验证 |

---

## Code Review 修复跟踪

| 问题 | 级别 | 状态 | 修复内容 |
|------|------|------|---------|
| Major-1: filepath.Glob 方括号注入 | Major | **已修复** | 改用 os.ReadDir + strings.HasPrefix |
| Major-2: qualityScore m4a 高码率分支 | Major | 保留 | 功能合理，补充设计文档即可 |
| Major-3: EnqueueUpgrade 无锁读 candidates | Major | 保留 | 当前安全（done 状态不会被并发写），M1 重构时处理 |
| Major-4: Errors 序列化为 null | Major | **已修复** | 初始化为 []UpgradeError{} |
| Minor-4: EOF 字符串匹配 | Minor | **已修复** | 改用 errors.Is(err, io.EOF) |

---

## 测试缺口

| 缺口 | 严重程度 | 建议 |
|------|---------|------|
| EnqueueUpgrade 无单元测试 | Minor | M0 scope 内补充 |
| Provider 回写集成测试 | Minor | 需真实 API 调用，改为手动验证 |
| 前端逻辑无自动化测试 | Info | Vanilla JS 项目无测试框架，手动验证 |
| .tmp 清理在下载失败时的行为 | Minor | 可补充单元测试 |

---

## 结论

**通过**。核心逻辑（qualityScore、WriteSongToDisk 三种 Action、QualityString）测试覆盖完整。Code Review 发现的 2 个阻塞问题（Glob 注入 + null 序列化）已修复并验证。剩余缺口（EnqueueUpgrade 测试、前端手动验证）不阻塞合并。
