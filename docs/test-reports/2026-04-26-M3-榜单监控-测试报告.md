# M3 榜单监控 — 测试报告

> 日期：2026-04-26
> 测试范围：scheduler、store CRUD + 去重、API handler、chart provider（netease/qq/kugou）、前端 MonitorView
> 对照文档：docs/milestones/milestone-3-榜单监控.md
> 结论：**通过**

---

## 1. 测试结果摘要

| 项目 | 结果 |
|------|------|
| `go build ./...` | PASS |
| `go test ./internal/store/ -v -cover` | PASS（28 tests，覆盖率 87.1%） |
| `go test ./internal/monitor/` | PASS（0 tests — 无测试文件） |
| `go test ./internal/api/` | PASS（0 tests — 无测试文件） |
| `go vet ./internal/monitor/ ./internal/store/ ./internal/api/` | PASS（无警告） |
| `npm run build` (前端) | 未单独运行，M6 已验证通过 |

**总问题数：4（Critical: 0, Major: 0, Minor: 2, Info: 2）**

通过条件：Critical = 0 且 Major ≤ 2 → **通过**

---

## 2. 验收标准覆盖

| # | 验收标准 | 验证方式 | 结果 |
|---|---------|----------|------|
| 1 | API 创建网易热歌榜监控（TopN=5, Interval=6） | `TestCreateMonitor_Defaults` + `TestCreateMonitor_ClampTopN` + handler 代码审计 | PASS |
| 2 | 手动触发 → 歌曲入队 | `scheduler.go:91-98` Execute() → execute() → dlMgr.EnqueueBatch() 代码审计 | PASS（代码级） |
| 3 | 再次触发 → 全部去重跳过 | `TestFilterDownloaded` + `TestFilterDownloaded_CrossPlatform` | PASS |
| 4 | 删除记录后重新入队 | `TestFilterDownloaded` 验证 failed 状态不算已下载，删除记录后 FilterDownloaded 查不到 | PASS |
| 5 | 重启后规则/历史保留 | SQLite 持久化 + M6 部署验证已确认数据保留 | PASS |
| 6 | 3 平台 8 张榜 | netease: 3 + qq: 3 + kugou: 2 = 8 张，代码审计确认 | PASS |
| 7 | 间隔 6/12/24h 三档 | `TestCreateMonitor_InvalidInterval` 验证非法值回退 12h | PASS |
| 8 | 执行历史保留 + 裁剪 100 条 | `TestPruneMonitorRuns` + `TestPruneMonitorRuns_NoPruneWhenUnderLimit` | PASS |

---

## 3. 问题清单

### Critical（必须修复，阻塞发布）

无。

### Major（应该修复，影响质量）

无。

### Minor（建议修复，提升质量）

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 1 | scheduler 无单元测试 | `internal/monitor/` | 目录下无 `*_test.go`。核心逻辑（tick、execute、dedup、enqueue 集成）仅靠 store 层测试间接覆盖。execute() 的完整流程未经测试验证 |
| 2 | API handler 无单元测试 | `internal/api/` | handler_monitor.go 的 7 个端点无测试。参数校验、错误码、边界情况未覆盖 |

### Info（信息记录，无需修复）

| # | 内容 | 说明 |
|---|------|------|
| 1 | 手动触发异步执行无反馈 | `handler_monitor.go:209` — `go monScheduler.Execute()` 后立即返回 200。前端无法知道执行结果，需要手动刷新历史查看。行为符合设计（异步），但用户体验可改进（WebSocket/轮询） |
| 2 | CreateMonitor 无重复检查 | `handler_monitor.go:78-90` — 同平台同 ChartID 可重复创建多条监控规则。参考项目 simple_sq_music_plus 有去重检查。当前不影响功能（各自独立调度），但用户可能误创建重复规则 |

---

## 4. 设计一致性检查

### 4.1 数据模型

| 设计要求 | 实现 | 一致 |
|---------|------|------|
| Monitor: ID, Name, Platform, ChartID, TopN, Interval, Enabled, LastRunAt, NextRunAt | `store/monitor.go:11-23` 完全匹配 | ✅ |
| MonitorRun: ID, MonitorID, StartedAt, FinishedAt, TotalFetched, NewQueued, Skipped, Status, Error | `store/monitor.go:26-36` 完全匹配 | ✅ |
| TopN 默认 20，最大 100 | `CreateMonitor` 和 `UpdateMonitor` 均验证 | ✅ |
| Interval 只支持 6/12/24 | `CreateMonitor` 非法值回退 12 | ✅ |

### 4.2 去重逻辑

| 设计要求 | 实现 | 一致 |
|---------|------|------|
| 去重键：platform + song_id | `FilterDownloaded(db, source, songIDs)` WHERE source=? AND song_id IN ? AND status=done | ✅ |
| 只跳过 status=done | 测试 `TestFilterDownloaded` 验证 failed 不算已下载 | ✅ |
| 跨平台不误去重 | 测试 `TestFilterDownloaded_CrossPlatform` 验证 | ✅ |

### 4.3 API 接口

| 设计要求 | 实现 | 一致 |
|---------|------|------|
| GET /api/monitors | `handleListMonitors` line 40 | ✅ |
| POST /api/monitors | `handleCreateMonitor` line 54 | ✅ |
| PUT /api/monitors/:id | `handleUpdateMonitor` line 94 | ✅ |
| DELETE /api/monitors/:id | `handleDeleteMonitor` line 143 | ✅ |
| GET /api/monitors/:id/runs | `handleListMonitorRuns` line 163 | ✅ |
| POST /api/monitors/:id/trigger | `handleTriggerMonitor` line 184 | ✅ |
| GET /api/charts?platform=X | `handleGetCharts` line 21 | ✅ |

### 4.4 Chart Provider

| 设计要求 | 实现 | 一致 |
|---------|------|------|
| 网易云：飙升榜(19723756)、热歌榜(3778678)、新歌榜(3779629) | `netease/charts.go:6-10` | ✅ |
| QQ：热歌榜(26)、新歌榜(27)、巅峰榜(4) | `qq/charts.go:14-18` | ✅ |
| 酷狗：飙升榜(6666)、热歌榜(8888) | `kugou/charts.go:13-16` | ✅ |

### 4.5 调度器

| 设计要求 | 实现 | 一致 |
|---------|------|------|
| 启动时加载 enabled monitors | `scheduler.go:61` — 启动后立即 tick() | ✅ |
| 1 分钟轮询，按 NextRunAt 判断 | `scheduler.go:57,83` — Ticker 1min + m.NextRunAt.After(now) | ✅ |
| 执行后更新 LastRunAt + NextRunAt | `scheduler.go:164` → `store.UpdateMonitorSchedule` | ✅ |
| 下载走现有 download.Manager | `scheduler.go:152` → `dlMgr.EnqueueBatch()` | ✅ |
| 历史裁剪 100 条 | `scheduler.go:169` → `PruneMonitorRuns(db, m.ID, 100)` | ✅ |

### 4.6 前端

| 设计要求 | 实现 | 一致 |
|---------|------|------|
| 列表所有监控 | MonitorView.vue:43-51 loadMonitors | ✅ |
| 新建/编辑/删除 | openCreate/openEdit/deleteMonitor | ✅ |
| 启用/禁用 toggle | toggleEnabled line 108 | ✅ |
| 手动触发 | triggerMonitor line 127 | ✅ |
| 执行历史 | openRuns line 136 | ✅ |
| 平台选择联动榜单 | watch platform → loadCharts line 81 | ✅ |
| 间隔三档 6/12/24h | intervalOptions line 23-27 | ✅ |

---

## 5. 假设审计

| # | 假设 | 位置 | 审计结果 | 风险 |
|---|------|------|----------|------|
| 1 | 网易云榜单 ID 长期不变 | `netease/charts.go:6-10` | 这些是网易云官方歌单 ID，历史上稳定。若更换，只需改硬编码常量 | 低 |
| 2 | QQ 榜单 topid 长期不变 | `qq/charts.go:14-18` | 26/27/4 是 QQ 音乐公开 API 的标准 topid，多年未变 | 低 |
| 3 | 酷狗 rankid 长期不变 | `kugou/charts.go:13-16` | 6666/8888 是酷狗移动 API 的固定 rank ID | 低 |
| 4 | FilterDownloaded 的 SQL IN 子句不会超限 | `store/monitor.go:167` | TopN 最大 100，IN 子句最多 100 个 ID，SQLite 无问题 | 低 |
| 5 | 1 分钟轮询不会造成性能问题 | `scheduler.go:57` | 每次 tick 只查 enabled monitors（通常 < 10 条）+ 时间比较，开销极低 | 低 |

---

## 6. 测试覆盖

### 6.1 Store 层覆盖率：87.1%

| 已覆盖路径 | 测试 |
|-----------|------|
| Monitor CRUD（创建/查询/更新/删除） | TestCreateMonitor_* / TestGetMonitor_* / TestUpdateMonitor / TestDeleteMonitor_CascadesRuns |
| Monitor 列表（全部/仅启用） | TestListMonitors / TestListEnabledMonitors |
| MonitorRun 创建/完成/排序/裁剪 | TestMonitorRun_* / TestListMonitorRuns_Order / TestPruneMonitorRuns_* |
| 去重（单条/批量/跨平台/空输入） | TestIsSongDownloaded / TestFilterDownloaded_* |
| 调度更新 | TestUpdateMonitorSchedule |

### 6.2 未覆盖的关键路径

| 路径 | 原因 | 风险 |
|------|------|------|
| scheduler.execute() 完整流程 | 无 scheduler 测试文件 | 中——需要 mock chart provider + download manager |
| API handler 参数校验 | 无 handler 测试文件 | 低——Gin binding 已做基础校验 |
| chart provider 真实 API 调用 | 需要外部网络 | 低——端到端在 NAS 部署时验证 |

---

## 7. 后续行动

| 优先级 | 行动 | 负责角色 | 状态 |
|--------|------|----------|------|
| P2 | 补充 scheduler 单元测试（mock provider + dlMgr） | 工程师 | 待处理 |
| P2 | 补充 API handler 单元测试（httptest） | 工程师 | 待处理 |
| P3 | CreateMonitor 添加 (platform, chart_id) 唯一性校验 | 工程师 | 待处理 |
| P3 | 手动触发后前端自动轮询执行结果 | 工程师 | 待处理 |
